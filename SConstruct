
import pkgboot

class Coro(pkgboot.Package):
    defines = {}
    includes = []
    libs = [
        pkgboot.Lib('ws2_32', 'win32'),
    ]
    major_version = '0'
    minor_version = '0'
    patch = '0'

Coro()
